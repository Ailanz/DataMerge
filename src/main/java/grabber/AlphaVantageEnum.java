package grabber;

public class AlphaVantageEnum {
    public enum Function {
        TIME_SERIES_DAILY,
        ADX
    }

    public enum OutputSize {
        FULL,
        COMPACT
    }

    public enum Interval {
        DAILY
    }
}
